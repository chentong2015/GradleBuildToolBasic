plugins {
    id 'java'
}

group 'org.example'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
}


// TODO. 在Build项目时自动调用方法
print(project.project.name)
print(project.project.group)
print(project.projectDir.toString())
